// JS for start page (index.html)

document.addEventListener("DOMContentLoaded", () => {
  console.log("✅ Shield Cheats loaded.");
  
  // Optional: focus the Login button on page load
  const loginBtn = document.querySelector(".btn");
  if (loginBtn) {
    loginBtn.focus();
  }

  // Optional: display date or dynamic content later here
});
